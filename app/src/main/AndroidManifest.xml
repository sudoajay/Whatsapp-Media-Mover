<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sudoajay.whatsapp_media_mover_to_sdcard"
    tools:ignore="ExtraText">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.REAL_GET_TASKS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>


    <application
        android:allowBackup="true"
        android:allowTaskReparenting="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.SplashScreen.WelcomeActivity"
            android:configChanges="orientation"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.After_MainTransferFIle"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.Main_Navigation"
            android:configChanges="orientation"

            android:label="@string/title_activity_main__navigation"
            android:screenOrientation="portrait"/>

        <activity android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.Show_Duplicate_File" />

        <service android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.Firebase_Message_Config.MyFireBaseInstanceMessageService"
            tools:ignore="InnerclassSeparator"
            android:stopWithTask="false"
            android:exported="false">
            <intent-filter>

                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:enabled="true" android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.ForegroundService.Foreground"
            tools:ignore="InnerclassSeparator" />


        <service android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.Receive_Boot_Completed.ForegroundServiceBoot"
            tools:ignore="InnerclassSeparator" />

        <receiver android:name="com.sudoajay.whatsapp_media_mover_to_sdcard.Receive_Boot_Completed.MyRebootReceiver"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>